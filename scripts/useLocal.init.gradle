println("using useLocal.init.gradle")

allprojects {
    buildscript {
        repositories {
            mavenLocal()
        }
    }
    repositories {
        mavenLocal()
    }

    tasks.all {
        if (it instanceof org.gradle.plugins.signing.Sign) {
            it.enabled = false
        }
    }
}
